# 08. Задачник — Агент создания контента (Агент 4)

Чек-лист выполненных и оставшихся этапов работы, по аналогии с `9. Задачник.md` Агента 3. Разворачивается
по мере работы — каждая выполненная задача отмечается здесь. Развёрнутое описание того, что именно
сделано в каждом слайсе — по коммитам в `Code/`.

> **Блокирующая внешняя зависимость:** Слайс 1 не может быть проверен сквозным живым прогоном, пока не
> реализована доработка D для Агента 1 (`Доработки для Агентов 1 и 3 (передать).md`) — новая таблица
> `intelligence_agent.agent4_handoff_queue` + публикация в `notifications:agent4`. До этого момента
> Слайс 1 пишется и тестируется на фейковых данных (по образцу `fakeSupabase`/`fakeRedis`, тот же
> паттерн, что у Агента 3), не на реальной интеграции с Агентом 1.

---

## ✅ Слайс 1 — Каркас проекта + приём задачи от Агента 1 (код готов, 2026-07-10)

- [x] Скаффолдинг проекта (`package.json`, `node:test`, консистентно с Агентами 1/2/3 — Node.js)
- [x] Supabase client factory, схема `content_creation_agent` (`src/db/client.js`, 3 теста)
- [x] Миграция: таблицы `generated_content`, `processed_wizard_requests`, `inbox_events`
      («07. Архитектура», §6) — применена к живой Supabase (`content_creation_agent_schema`,
      подтверждено `list_tables`, RLS включён на всех трёх)
- [x] Разбор событий из `notifications:agent4` по полю `event` (`wizard_ready` | `digest_ready`) —
      `src/inbox/parseEvent.js`, 5 тестов
- [x] Реальная Redis-подписка — `src/inbox/subscribe.js`, 5 тестов (`ioredis`, `_redis`-инъекция для
      тестов по образцу `agent4Handoff.js` Агента 3)
- [x] Поллинг `intelligence_agent.agent4_handoff_queue` (catch-up механизм) — `src/inbox/
      pollHandoffQueue.js` (чтение) + `src/inbox/poller.js` (start/stop таймер-обёртка, интервал
      30 сек по умолчанию), 3+3 теста
- [x] Кросс-схемное чтение `intelligence_agent.users.settings` по `telegram_id` — `src/ingestion/
      agent1Reader.js`, 4 теста, `.schema('intelligence_agent')` подтверждён через `db.schemaCalls`
- [x] `computeWizardHash` — `src/wizard/hash.js`, 3 теста (детерминизм, чувствительность к
      изменению полей, независимость от порядка ключей)
- [x] Сверка `wizard_hash` из очереди с `hash(wizard)` из прочитанного объекта — `src/inbox/
      intake.js`, при расхождении используется актуальная версия (лог-предупреждение), 5 тестов
- [x] Дедупликация через `processed_wizard_requests` (`UNIQUE(telegram_id, wizard_hash)`) —
      `src/dedup/processedWizardRequests.js`, 4 теста (включая non-throw на 23505 unique-violation
      при гонке). Отметка "обработано" сознательно НЕ ставится в Слайсе 1 — только проверяется;
      реальную отметку ставит будущий слайс генерации после успеха, см. комментарий в `intake.js`
- [x] Fake Supabase/Redis для тестов (`tests/helpers/fakeSupabase.js` — копия паттерна Агента 3 1-в-1,
      `tests/helpers/fakeRedis.js` — новый, поддержка `subscribe`/`on('message')`/`publish`)
- [x] `src/index.js` — точка входа, соединяющая всё в рабочий цикл; `onJob` — явная точка расширения
      для будущего роутера генерации (Слайс 2+), пока логирует и не генерирует

**Статус тестов на 2026-07-10:** `npm test` → **35/35 зелёных**, вывод чистый. Всё — через строгий
TDD (RED подтверждён перед каждой реализацией).

**Обновление 2026-07-10:** доработка D на стороне Агента 1 **реализована и проверена живьём**
(коммит `ee038df`, v0.2.6) — реальная таблица `intelligence_agent.agent4_handoff_queue` подтверждена
через `list_tables` (колонки 1-в-1 с тем, что ожидает код Агента 4). Сквозной живой прогон теперь
технически возможен, как только будет настроен `.env` с реальными ключами — не проверялся в этой
сессии (не заводился реальный `.env`, только `.env.example`).

## ✅ Слайс 2 — Роутер генерации + каскад для текста (код готов, 2026-07-10)

- [x] Роутер по `wizard.content_type` (text | image | video | audio) — `src/router/route.js`, 5
      тестов. Реализован только `text`; image/video/audio (Слайсы 4-6) бросают понятную ошибку
      "not implemented yet"
- [x] Каскад для текста: дешёвая LLM (`claude-haiku-4-5`) → основная (`claude-sonnet-4-6`) при сбое/
      пустом ответе — `src/generation/text/cascade.js`, 6 тестов. OpenRouter + опциональный Helicone-
      прокси, 1-в-1 паттерн `extractClaims.js` Агента 3 (`fetchImpl`-инъекция, `usage.cost`)
- [x] Порог автоподтверждения $0.50 — `src/cost/threshold.js`, 4 теста. Общий строительный блок;
      для текста pre-flight оценка не нужна (стоимость на 2-3 порядка ниже порога), пригодится в
      Слайсах 4-5 (видео/фото с известной ценой за операцию)
- [x] Запись результата в `generated_content` (status: pending → processing → done|error) —
      `src/generation/persistence.js`, 5 тестов
- [x] Оркестратор, связывающий роутер+каскад+запись — `src/generation/generate.js`, 3 теста.
      Подключён в `src/index.js` вместо стаб-заглушки Слайса 1 (`onJob` теперь реально генерирует
      текст, не только логирует)

**Статус тестов на 2026-07-10 (после Слайса 2):** `npm test` → **58/58 зелёных**, вывод чистый.

**Не проверено живым вызовом** (только на фейковом `fetchImpl`, реальный OpenRouter не вызывался) —
нет заведённого `.env` с реальным `OPENROUTER_API_KEY` в этой сессии.

## ✅ Слайс 3 — Хранилище (Cloudflare R2, код готов, 2026-07-10)

- [x] R2-клиент — `src/storage/r2Client.js`: `uploadFile()` (PutObjectCommand) + `getSignedDownloadUrl()`
      (подписанная, ограниченная по времени ссылка через `@aws-sdk/s3-request-presigner` — bucket не
      настроен как публичный, кастомный домен не привязан, поэтому доставка через presigned URL, не
      прямую публичную ссылку). Пакеты `@aws-sdk/client-s3`/`@aws-sdk/s3-request-presigner` добавлены
      в `package.json`. 5 тестов (инъекция `_s3`/`_getSignedUrl`, без реального обращения к Cloudflare)
- [x] `persistence.markDone` расширен опциональным `r2Url` (по умолчанию `null` — текстовая генерация
      не хранит файлы, так и остаётся `null`) — 2 новых теста
- [x] `generation/generate.js` прокидывает `result.r2Url` из каскада в `markDone` — 1 новый тест,
      готово для Слайсов 4-6 (image/video/audio каскады будут возвращать `r2Url` после `uploadFile`)
- [x] `src/index.js` — R2-клиент собирается **опционально**: если все 4 переменные
      (`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET`) заданы — создаётся и
      прокидывается в `routeDeps.r2`; если нет — предупреждение в лог, текстовая генерация (Слайс 2)
      продолжает работать без сбоев
- [x] Настройка bucket + API-токен R2 в самом Cloudflare — выполнено пользователем 2026-07-10,
      bucket `marketing-agency-content-creation-agent`, вписан в `.env`/`R2_BUCKET`
- [x] `src/storage/createStorageClient.js` — фабрика выбора backend'а хранилища через
      `STORAGE_PROVIDER` (по умолчанию/пока единственно реализованный — `'r2'`). Контракт
      (`uploadFile`/`getSignedDownloadUrl`) уже соблюдён `r2Client.js`, каскады (Слайсы 4-6)
      по-прежнему ничего не знают о конкретном backend'е — получают его через DI. `index.js`
      теперь создаёт хранилище через эту фабрику, а не напрямую `createR2Client`. 3 теста.
      Не более чем "шов" под будущую замену — гипотетические backend'ы (Supabase Storage и
      т.д.) не реализуются заранее, только когда появится конкретная причина
- [ ] Политика хранения (лимит 10 ГБ, предупреждение пользователя + согласие на удаление
      старого контента) — решение принято (см. ниже, «Транспорт Агент4→Агент1»), реализация
      квоты откладывается до появления самого канала связи с пользователем; область действия
      (на пользователя vs. на весь bucket) — открытый вопрос, требует ответа

**Статус тестов на 2026-07-10 (после Слайса 3):** `npm test` → **66/66 зелёных**, вывод чистый.

**R2 не проверено живым вызовом** — bucket и API-токен R2 в Cloudflare не созданы (нужно сделать
вручную, после чего возможна живая проверка `uploadFile`/`getSignedDownloadUrl`).

## ✅ Живая сквозная проверка Слайсов 1+2 (2026-07-10) — прогнано по-настоящему, не на фейках

Заведён реальный `Code/.env` (Supabase/Redis — общие для всех 4 агентов, ключ переиспользован из
`Information analysis agent/Code/.env`; `OPENROUTER_API_KEY`/`HELICONE_API_KEY` — реальные ключи из
`Project settings/API Keys.txt`). Создан временный тестовый пользователь
(`telegram_id 999000001`, заведомо не пересекается с реальным пользователем `1064521326`) с
wizard-настройками и записью в `intelligence_agent.agent4_handoff_queue`, агент запущен по-настоящему
(`node src/index.js`). Результат: поллинг забрал задачу → кросс-схемное чтение wizard'а →
дедуп-проверка → каскад текста (реальный вызов OpenRouter, `claude-haiku-4-5`, эскалация не
понадобилась) → запись в `generated_content` со статусом `done`, реальная стоимость **$0.001622**,
готовый текст поста на русском. Тестовые данные (пользователь, очередь, результат) удалены после
проверки, реальный пользователь не тронут.

**Найдено и исправлено живой проверкой — два реальных бага, оба не ловились тестами на фейках:**

1. **Отсутствовал `GRANT USAGE ON SCHEMA content_creation_agent`** — миграция 001 давала `GRANT` только
   на таблицы, но в Postgres это не работает без `GRANT USAGE ON SCHEMA` в дополнение (у остальных 3
   агентов это было сделано, здесь — забыто). Ошибка: `permission denied for schema
   content_creation_agent`. Исправлено новой миграцией `002_schema_grant.sql`, применена к живой БД.
2. **Реальный `supabase-js` `.single()` на пустом результате возвращает ошибку `PGRST116`
   ("Cannot coerce the result to a single JSON object"), а не `{data:null, error:null}`**, как
   ошибочно предполагал фейковый `makeFakeDb` при написании тестов Слайса 1. Ломало
   `isWizardProcessed` (упало бы на первом же новом, ещё не обработанном wizard-запросе) и
   `fetchWizardSettings` (упало бы на несуществующем `telegram_id`). Добавлен общий хелпер
   `src/db/errors.js` (`isNotFoundError`), тесты в обоих местах переписаны под реальное поведение
   PostgREST, добавлен новый тест `db/errors.test.js`.

**Статус тестов после фиксов:** `npm test` → **69/69 зелёных**.

## ✅ Слайс 4 — Каскад генерации изображений (код готов на фейках, 2026-07-10)

- [x] `src/generation/image/cascade.js` — Runway (`gen4_image_turbo` дешёвый тир → `gen4_image`
      эскалация при HTTP-сбое или `status: FAILED`). Асинхронный API: `POST /v1/text_to_image` →
      `{id}` → поллинг `GET /v1/tasks/:id` до `SUCCEEDED`/`FAILED`/таймаута → скачивание эфемерной
      ссылки результата (истекает за 24-48ч, подтверждено официальной документацией Runway) →
      загрузка в R2 через уже готовый `r2Client`. 9 тестов
- [x] Точный контракт запроса (`model`/`promptText`/`ratio`, заголовки `Authorization`/
      `X-Runway-Version: 2024-11-06`) и формат ответа задачи (`output: [...]`) — сверены с реальной
      документацией Runway (docs.dev.runwayml.com), не придуманы
- [ ] **Неподтверждённое предположение, требует проверки на первом живом вызове:** документация
      показывает `referenceImages` как обязательное поле в развёрнутой по умолчанию вкладке UI для
      `gen4_image_turbo`, что нетипично для чистого text-to-image — в коде это поле НЕ передаётся
      (у wizard'а сейчас нет референсных изображений). Если реальный вызов вернёт 400 — это первое,
      что нужно перепроверить
- [x] `routeByContentType` теперь принимает **namespaced deps** (`deps.text`/`deps.image`) вместо
      одного плоского объекta — до этого text (OpenRouter) и image (Runway) читали бы `apiKey` из
      одного и того же объекта, что перепутало бы ключи при реальном использовании. Пойман до живого
      вызова, регрессионный тест добавлен (`route.test.js`, "does not leak deps.text.apiKey...")
- [x] `generate.js` прокидывает `job.telegram_id` в объект wizard, передаваемый генератору — нужно
      для именования файла в R2 (`{telegram_id}/{timestamp}-image.png`)
- [x] `index.js` — `RUNWAY_API_KEY` опционален (как и R2): без него image-задачи будут падать в
      `createImageCascade` с понятной ошибкой, оркестратор пометит запись как `error`, text
      продолжает работать

**Стоимость:** Runway не отдаёт точную стоимость в ответе задачи (только через отдельный
account-usage endpoint) — в отличие от OpenRouter, который даёт `usage.cost` напрямую. Использована
приблизительная оценка по тиру из «02. Анализ» ($0.02 дешёвый / $0.20 основной), не точная метрика.

**Статус тестов на 2026-07-10 (после Слайса 4):** `npm test` → **80/80 зелёных**.

**Не проверено живым вызовом** — ждём правильные R2-ключи (без них живая проверка невозможна, т.к.
каскад изображений требует R2 для загрузки результата) и рабочий `RUNWAY_API_KEY`.

## ✅ Слайс 5 — Каскад генерации видео (код готов на фейках, 2026-07-10)

- [x] `src/generation/video/cascade.js` — **оба тира на MiniMax** (не Runway/Sora 2, как было в
      черновике ТЗ — см. ниже почему), `MiniMax-Hailuo-02`/768P дешёвый → `MiniMax-Hailuo-2.3`/1080P
      основной. Три реальных HTTP-шага (подтверждено документацией platform.minimax.io):
      `POST /v1/video_generation` → `{task_id}` → поллинг `GET /v1/query/video_generation?task_id`
      до `status: Success`/`Fail`/таймаута → `GET /v1/files/retrieve?file_id` → `download_url` →
      скачивание и загрузка в R2. 9 тестов
- [x] **Осознанное отклонение от «05. ТЗ» §4.1/§6** (там указаны MiniMax/Runway → Sora 2/Runway):
      1) OpenAI Sora 2 объявлен deprecated, отключается 24 сентября 2026 — интегрировать новую
      зависимость от API с истекающим сроком жизни бессмысленно; 2) видео у Runway доступно только
      как `image_to_video` (`promptImage` обязателен) — не подходит для чистого текстового промпта
      без отдельного пайплайна «сначала картинка, потом видео из неё». Оба вывода — из реальной
      документации (docs.dev.runwayml.com, developers.openai.com), не предположения
- [x] `routeByContentType` — добавлен кейс `video` → `deps.video` (namespaced, как и `image`),
      регрессионный тест на изоляцию от `deps.image`
- [x] `index.js` — `MINIMAX_API_KEY` опционален (как `RUNWAY_API_KEY`/R2)

**Стоимость:** MiniMax тоже не отдаёт точную стоимость в ответе задачи — приблизительная оценка по
модели из «02. Анализ» ($0.10 дешёвый / $0.28 основной), не точная метрика.

**Статус тестов на 2026-07-10 (после Слайса 5):** `npm test` → **89/89 зелёных**.

**Не проверено живым вызовом** — ждём R2-ключи и рабочий `MINIMAX_API_KEY`.

## ✅ Слайс 6 — Каскад генерации аудио (код готов на фейках, 2026-07-10)

- [x] `src/generation/audio/cascade.js` — Deepgram Aura-2 (дешёвый) → ElevenLabs (основной,
      фирменный голос) при сбое. **Обе API синхронные** — в отличие от Runway/MiniMax (Слайсы 4-5),
      один POST сразу возвращает готовые аудио-байты, без задачи/поллинга — самый простой из четырёх
      каскадов. Контракт сверен с реальной документацией (developers.deepgram.com,
      elevenlabs.io/docs/api-reference). 9 тестов
- [x] Стоимость считается по длине текста (`(text.length/1000) * ставка`), не по фиксированному
      тиру, как у Runway/MiniMax — это ближе к реальному биллингу обоих провайдеров (посимвольная
      тарификация), хоть ставки всё равно приблизительные (из «02. Анализ»)
- [x] `elevenLabsVoiceId` — обязателен, **без дефолта**: клонирование фирменного голоса бренда ещё
      не настроено, осознанно не подставлял угаданный voice_id вместо реального
- [x] `routeByContentType` — добавлен кейс `audio` → `deps.audio`, регрессионный тест на изоляцию от
      `deps.video`. **Все 4 типа контента MVP (текст/фото/видео/аудио) теперь реализованы** —
      сообщение об ошибке для неизвестного типа изменено с "not implemented yet" на
      "unknown content_type" (соответствующий тест обновлён)
- [x] `index.js` — `DEEPGRAM_API_KEY`/`ELEVENLABS_API_KEY`/`ELEVENLABS_VOICE_ID` опциональны, как и
      остальные ключи генерации

**Статус тестов на 2026-07-10 (после Слайса 6):** `npm test` → **98/98 зелёных**.

**Не проверено живым вызовом** — ждём R2-ключи и рабочие `DEEPGRAM_API_KEY`/`ELEVENLABS_API_KEY`/
`ELEVENLABS_VOICE_ID` (последний — реальный ID голоса, ещё не выбран).

---

### MVP по генерации контента (Слайсы 2, 4-6) — код готов полностью

Все 4 типа контента из «04. Брейншторм», §1 (текст/фото/видео/аудио) имеют рабочий каскад
дёшево→дорого, протестированы на фейках. Живая проверка прошла пока только для текста (Слайс 2,
реальный OpenRouter). Изображения/видео/аудио дополнительно требуют R2 (для загрузки результата) —
единственная всё ещё не устранённая внешняя зависимость, блокирующая живую проверку оставшихся трёх
каскадов (реальные ключи R2 получены 2026-07-10, но сеть на машине разработчика не пропускает TLS до
`*.r2.cloudflarestorage.com` — см. отдельную заметку ниже, живая проверка отложена до починки сети).
Дальше по плану — Слайс 8 (публикация), Слайс 9 (доставка Агенту 1), Слайс 10 (собственный
MCP-сервер), Слайс 11 (Docker-деплой), Слайс 12 (стоимость/observability).

**Заметка про R2 (2026-07-10):** пользователь прислал реальные `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/
`R2_SECRET_ACCESS_KEY`, сохранены в `Code/.env`. Живая проверка (`ListBucketsCommand`) не прошла —
`SSL alert number 40` (TLS handshake failure), причём DNS на машине резолвит `*.r2.cloudflarestorage.com`
(и вообще любые домены) в тестовый диапазон `198.18.0.0/15` — похоже на локальный VPN/защитное ПО,
перехватывающее трафик по SNI и не пропускающее именно R2. Не проблема с кодом или ключами — сетевая
конфигурация конкретной машины. `R2_BUCKET` тоже пока не указан (не удалось выполнить `ListBuckets`,
чтобы узнать имя автоматически).

## ✅ Слайс 7 — Обогащение трендовыми данными (код готов на фейках, 2026-07-10)

- [x] `src/enrichment/classifyTrendRequest.js` — `wantsTrendEnrichment(description)`, эвристика по
      ключевым словам (RU/EN), не LLM-вызов: для бинарного да/нет отдельный LLM-запрос был бы overkill
      по стоимости/задержке. Если на практике окажется ненадёжной — заменить на LLM без смены
      сигнатуры функции. 6 тестов
- [x] `src/mcp-clients/informationAnalysisClient.js` — реальный MCP-клиент к Агенту 3 (`analysis_digest`/
      `analysis_detail`, порт 7302, путь `/mcp`), точное зеркало паттерна `deepParsingClient.js`
      самого Агента 3 (сверено чтением его исходников — тот же `ClientImpl`/`TransportImpl` seam,
      тот же способ разбора `content[0].text`). 8 тестов
- [x] `src/enrichment/rawDataReaders.js` — `fetchRawSearchResult`/`fetchRawParsingResult`, fallback на
      сырые данные Агента 1/2 при недостаточности дайджеста, кросс-схемное чтение с обязательным
      `.schema(...)`. 4 теста
- [x] `src/enrichment/enrichWithTrends.js` — связывает всё: дайджест → детали факта → `raw_job_id`
      источника → сырой результат парсинга Агента 2 → `combined_analysis` (`hooks/triggers/offers/
      viral_reasons/content_ideas`). **MVP-упрощение, явно отмеченное в коде:** берётся первый факт
      дайджеста и его первый источник, без семантического сопоставления с темой wizard-запроса —
      реальное сопоставление по теме прямо названо в «07. Архитектура», §5.2 как отдельная будущая
      задача ("LLM-фильтр внутри Агента 4"), не реализовано здесь. **Best-effort**: любая ошибка
      (Агент 3 недоступен и т.д.) — не бросает исключение, просто `null`, генерация продолжается без
      обогащения. 6 тестов
- [x] `generate.js` — принимает опциональный `enrich`, вызывает его до роутера и прокидывает результат
      как `wizard.trendContext` в объект, передаваемый генератору. 3 новых теста
- [x] `text/cascade.js` — `buildPrompt` включает секцию с хуком/триггерами/оффером/причиной
      виральности/идеями подачи, когда `trendContext` присутствует (Hook–Retention–Payoff, см. «02.
      Анализ», §2.4) — сохраняется механика референса, не его буквальное содержание. 2 новых теста.
      **Не подключено к image/video/audio каскадам** — их промпты сейчас проще (нет
      развёрнутого текстового промпта, как у текста), обогащение туда не добавлено в этом слайсе
- [x] `index.js` — `INFORMATION_ANALYSIS_AGENT_URL` опционален: без него `enrich` вообще не создаётся,
      `generateContent` просто ставит `trendContext: null`, обычная генерация не страдает

**Статус тестов на 2026-07-10 (после Слайса 7):** `npm test` → **127/127 зелёных**.

**Не проверено живым вызовом** — нужен реально запущенный MCP-сервер Агента 3 (порт 7302) с реальными
данными в дайджесте, содержащими `combined_analysis` от Агента 2 (нужен минимум один живой прогон всей
цепочки Агент1→2→3, а не только Агента 4 изолированно).

## ✅ Транспорт Агент4→Агент1 — core (код готов на фейках, 2026-07-10)

Вынесено вперёд нумерации: и Слайс 8 (подтверждение модерации), и Слайс 9 (отчёт о готовом
контенте), и квота R2 (см. Слайс 3 выше) — все три упираются в один и тот же нерешённый
транспорт. Решение (обсуждение с пользователем 2026-07-10): **вариант (а)** — зеркальная
очередь + Redis, по образцу уже реализованных хендоффов (Агент1→Агент4, Агент3→Агент4), а не
переиспользование `notifications:agent1` (тот канал уже занят семантикой Агента 2→Агент1,
независимый канал безопаснее — не завязан на чужой формат сообщений).

- [x] Миграция `003_agent1_delivery_queue.sql` — таблица `content_creation_agent.
      agent1_delivery_queue` (`telegram_id`, `message_type`, `generated_content_id` (nullable,
      FK на `generated_content`), `payload` jsonb, `attempt_count`/`last_attempt_at`/`status`/
      `created_at` — тот же костяк колонок, что у обоих `agent4_handoff_queue`). Применена к
      живой Supabase, подтверждена `list_tables`. `message_type` различает поводы обращения
      к Агенту 1 через один канал/таблицу: `content_ready` (Слайс 9), `quota_warning` (лимит
      R2), в будущем возможны другие (например `moderation_request` для Слайса 8)
- [x] `src/delivery/agent1Notifier.js` — `createAgent1Notifier`, точное зеркало
      `agent4Handoff.js` Агента 3 (два слоя: надёжный Supabase-insert с ретраями `[500,2000,
      8000]`мс + быстрый Redis pub/sub best-effort в канал `notifications:agent1_from_agent4`).
      7 тестов
- [x] Собран в `index.js` (`notifyAgent1`), пока без вызовов — первый потребитель появится в
      Слайсе 8/9

**Не сделано в этом шаге (осознанно, не блокирует остальное):** приёмная сторона на Агенте 1
(подписка на `notifications:agent1_from_agent4` + поллинг `content_creation_agent.
agent1_delivery_queue`) — это код в чужом репозитории, по установленному в проекте правилу
записывается в `Доработки для Агентов 1 и 3 (передать).md`, не реализуется здесь напрямую.

**Статус тестов на 2026-07-10:** `npm test` → **137/137 зелёных**.

## Слайс 8 — Публикация (PostMyPost)

- [ ] Клиент PostMyPost, маппинг типов контента на параметры API
- [ ] Условие: только при `mode == 'publish'`
- [ ] Чтение `moderation_mode` из `intelligence_agent.users.settings`
- [ ] `moderation_mode == true` → пауза, запрос подтверждения через Агента 1 (см. Слайс 9, механизм
      запроса — детализировать здесь при реализации)
- [ ] `moderation_mode == false` → публикация сразу
- [ ] Отчёт по каждой соцсети отдельно (успех/неудача+причина) → `generated_content.publish_report`

## Слайс 9 — Доставка отчёта пользователю (Агент 4 → Агент 1)

- [ ] Транспорт обратного канала — решить между: (а) зеркальная очередь
      `content_creation_agent.agent1_delivery_queue` + Redis, по аналогии с уже реализованными
      хендоффами; (б) переиспользование существующего `notifications:agent1` (канал, которым уже
      пользуется Агент 2 → Агент 1) — открытый вопрос «07. Архитектура», §9, закрыть в начале этого
      слайса
- [ ] Формирование короткого отчёта с пометками (соцсеть/тип/описание/размер) — не пачка файлов
- [ ] Превью/ссылки на R2-файлы в отчёте
- [ ] Обработка запроса пользователя "прислать конкретный файл" / "прислать всё" (реализуется на
      стороне Агента 1 при получении отчёта — уточнить границу ответственности при реализации)

## Слайс 10 — MCP-сервер Агента 4 (выход)

- [ ] MCP Streamable HTTP, порт 7303, путь `/mcp` (см. «07. Архитектура», §3)
- [ ] `content_generate({ request })`
- [ ] `content_status({ job_id })`
- [ ] `content_result({ job_id })`
- [ ] Точная сигнатура — финализировать при реализации (черновик в «05. ТЗ», §5.3)

## Слайс 11 — Docker + деплой

- [ ] `Code/docker-compose.yml` (образец — «07. Архитектура», §3, идентичен паттерну Агента 3)
- [ ] Подключение к `Инфраструктура (Docker)/docker-compose.yml` (раскомментировать строку 13)
- [ ] Проверка совместного поднятия с Агентами 1/2/3 на `marketing-agency-net`

## Слайс 12 — Наблюдаемость и логирование стоимости

- [ ] Логирование фактических трат по каждому запросу генерации (`cost_usd` в `generated_content`)
- [ ] LangSmith/Helicone — подключить по той же схеме, что и Агент 3 (см. его Задачник, «Наблюдаемость»
      — там же отмечено, что общая настройка для всех агентов отложена, уточнить актуальность)

---

## v2 (после MVP)

- [ ] Явный переключатель "опора на тренды" в wizard'е Агента 1 (вместо эвристики по свободному
      тексту) — если Слайс 7 на практике окажется ненадёжным
- [ ] Расширенная retention-политика R2 (сейчас — фиксированное N, см. Слайс 3)
- [ ] Расширенная аналитика эффективности контента постфактум (данные от соцсетей)
- [ ] LangGraph-оркестратор для сложных многошаговых цепочек генерации, если простого роутера
      (Слайс 2) окажется недостаточно

---

## Открытые вопросы, которые нужно закрыть до/во время реализации

Полный список — «05. ТЗ», §12 и «07. Архитектура», §9. Кратко, самое важное:

- ~~Транспорт Агент 4 → Агент 1 (Слайс 9)~~ — решено 2026-07-10, см. «Транспорт Агент4→Агент1 —
  core» выше (зеркальная очередь + Redis, канал `notifications:agent1_from_agent4`).
- Число N хранимых "про запас" файлов в R2 (Слайс 3) — решение по механике принято (лимит
  10 ГБ, предупреждение+согласие пользователя на удаление старого), но **область действия
  ещё не определена**: на каждого пользователя отдельно или общий лимит на весь bucket —
  требует ответа перед реализацией самой квоты.
- Периодичность поллинга входящих очередей (Слайс 1).

---

*Дата: 2026-07-10*
