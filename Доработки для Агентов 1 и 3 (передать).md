# Доработки для Агентов 1 и 3 (передать)

> Написано 2026-07-09 в ходе брейншторма Агента 4 (Content creation agent), файл
> `План разработки/04. Брейншторм.md`. Оба пункта ниже — предпосылки для полноценной работы Агента 4,
> но реализуются в чужих, уже частично/полностью сданных репозиториях — по правилу разделения зон
> ответственности между агентами это не делается из сессии Агента 4. Файл лежит в корне
> `Content creation agent/`, а не в папках Агента 1/3, чтобы не трогать чужую git-историю из этой
> сессии (тот же приём, что уже использовался Агентом 3 для своих `Доработки для Агента 1/2
> (передать).md`) — когда команда будет готова, перенести/скопировать нужную часть в
> соответствующий проект.

---

## ✅ A. Доработка Агента 1 (Intelligence agent) — третий режим + wizard — РЕАЛИЗОВАНО

> **Статус: сделано.** Коммит `cae087f` (v0.2.5, «Wizard: 3 режима + 5-шаговый wizard + модерация»).
> Реализация подтверждена чтением кода при написании «07. Архитектура (Бекенд).md» Агента 4 — три
> режима (`mode_info`/`mode_content`/`mode_publish`), 5-шаговый wizard, `moderation_mode` — всё на
> месте. Оставлено ниже для истории, не требует повторной работы.

### Что сейчас

`Intelligence agent/План разработки/01. Идея.md` описывает только **два** режима выбора при `/start`:
«🔍 Получить информацию» (Агенты 1+2+3) и «🎬 Создать контент» (Агенты 1+2+3+4, помечен как «в
разработке — скоро»). Диалогового wizard с уточняющими вопросами (соцсеть/тип/формат/стиль) в текущих
документах Агента 1 нет вообще.

### Что найдено при разборе исходного мастер-проекта

`Marketing agency/План разработки/4. Архитектура.md` (Модуль 2 «Контент-завод», до разделения на 4
агента) уже содержал готовый дизайн, который не был перенесён при миграции на 4-агентную
Telegram-архитектуру:

- Диалоговый wizard из 5 шагов: 1) выбор соцсети (Instagram/YouTube/TikTok/Telegram/ВК/WhatsApp/
  Одноклассники) → 2) тип контента (пост/видео/фото/аудио/Reels) → 3) формат/соотношение сторон
  (9:16/16:9/1:1 и др.) → 4) стиль подачи → 5) свободное описание задачи.
- Отдельный «Агент-Публикатор» (в исходной 3-агентной схеме) с режимами **Автопилот** (публикует без
  подтверждения) и **Модерация** (ждёт подтверждения пользователя перед публикацией) — оба как
  переключатели, доступные в любой момент.

### Что нужно сделать

1. Добавить **третий режим** при выборе действия: 🚀 «Создать и опубликовать» (Агенты 1+2+3+4 +
   публикация через PostMyPost). Существующий режим «🎬 Создать контент» остаётся — теперь это
   «создать без публикации».
2. Для режимов 2 и 3 — включить диалоговый wizard (5 шагов выше). Для режима 3 дополнительно — выбор
   целевых соцсетей для публикации (мультивыбор).
3. Учесть режим модерации (вкл/выкл) как настройку пользователя, сохраняемую в `user_settings` (по
   аналогии с уже существующим `/mode`), — нужен ли он в MVP Агента 4 или сразу автопилот, решается на
   этапе ТЗ Агента 4, но переключатель на стороне Агента 1 должен существовать заранее.
4. `mode_content` — уже существующая UI-заготовка в Агенте 1 (кнопка есть, состояние пока не
   сохраняется даже частично, см. `Доработки для Агента 1 (передать).md` в `Information analysis
   agent/`, раздел A, пункт 4) — эта доработка расширяет её до полноценного состояния с 3 режимами и
   сохранением выбора wizard'а.

---

## ✅ B. Доработка Агента 3 (Information analysis agent) — push-хендофф к Агенту 4 — РЕАЛИЗОВАНО

> **Статус: сделано.** Коммит `71efe4a` (v0.7.0, «Agent4 handoff: Redis+Supabase push после
> дайджеста»). Подтверждено чтением кода: канал `notifications:agent4`, таблица
> `agent4_handoff_queue`, ретраи `[500,2000,8000]`мс на INSERT. Детали и **два новых пункта,
> вскрывшихся уже после реализации** — в разделах C и D ниже. Оставлено ниже для истории.

### Что сейчас

`Information analysis agent/План разработки/7. Архитектура (Бекенд).md`, §6.3 «Недоступность
Агента 4» — Агент 3 спроектирован как чистая **pull-модель**: «Не требует буфера/retry с нашей
стороны — модель pull означает, что Агент 4 просто не получит [данные], если сам недоступен».
Буфера/очереди на передачу Агенту 3 → Агенту 4 в коде нет (в отличие от Агента 2 → Агента 3, где есть
`deep_parsing_agent.agent3_handoff_queue`). Проект Агента 3 code-complete (все 10 слайсов ТЗ сданы).

### Что нужно сделать

Добавить push-инициацию по аналогии с уже существующим `handoffToAgent3()` у Агента 2:

1. **Новая таблица** `information_analysis_agent.agent4_handoff_queue` (Supabase) — по образцу
   `deep_parsing_agent.agent3_handoff_queue`: `id, job_id (= digest_id/run_id), result_ref,
   attempt_count, last_attempt_at, status, created_at`.
2. После завершения прогона (после узла `globalSynthesis.js`, когда дайджест уже сохранён в
   Supabase) — Агент 3 сам пишет уведомление в **Redis** (быстрый слой, по аналогии с
   `queue:deep-parsing:jobs`) и одновременно в новую таблицу выше (надёжный слой).
3. **In-process буфер с ретраями/бэкоффом** внутри Агента 3 — на случай, если сама запись в Redis/
   Supabase не удалась (сбой хранилища) — чтобы результат прогона не терялся молча.
4. Агент 4 читает уведомление из Redis/таблицы, забирает данные напрямую из Supabase или через уже
   существующий MCP (`analysis_digest`/`analysis_detail`) — этот код пишется уже на стороне Агента 4,
   не Агента 3.
5. **Регрессионный риск:** проект Агента 3 уже сдан и покрыт тестами — доработка должна пройти тем же
   путём (fakeSupabase с `db.schemaCalls`, regression-тесты на новый узел), которым уже проверялась
   аналогичная доработка `agent1Reader`/`agent2Reader` (см. `Доработки для Агентов 1 и 2 (передать).md`
   в этом же проекте).

---

## ✅ C. Доработка Агента 3 — актуализировать документацию под уже сданный код — РЕАЛИЗОВАНО (2026-07-09)

> **Статус: сделано.** Коммит `c4b618d` в `Information analysis agent`. §4.1/§4.3/§6.3 обновлены,
> описывают фактическую push+pull гибридную модель (см. пункт D выше). Оставлено ниже для истории.

> Написано 2026-07-09 при работе над «07. Архитектура (Бекенд)» Агента 4. Не блокирует Агента 4 —
> код первичнее прозы, Агент 4 спроектирован по коду, не по документу. Чисто документальная правка,
> без риска для функциональности. Можно делать не срочно, когда откроется следующая сессия по Агенту 3.

### Что найдено

`Information analysis agent/План разработки/7. Архитектура (Бекенд).md` не обновлён после коммита
`71efe4a` (пункт B выше) и противоречит собственному коду проекта:

- §4.1: «Агент 3 ничего не "толкает" наружу, кроме Telegram-уведомлений напрямую пользователю» — уже
  не так, есть `notifications:agent4` + `agent4_handoff_queue`.
- Диаграмма (строки ~152–154): показывает `[Агент 4] ──MCP: analysis_digest()──▶ [Агент 3]` как
  единственное направление — нужно добавить встречную стрелку push-уведомления.
- §6.3 «Недоступность Агента 4»: «Не требует буфера/retry с нашей стороны — модель pull означает, что
  Агент 4 просто не получит данные» — прямо противоречит уже реализованным буферу и ретраям.

### Что нужно сделать

Обновить §4.1/диаграмму/§6.3 в `7. Архитектура (Бекенд).md`, описав фактическую **push+pull
гибридную** модель: Агент 3 толкает лёгкое уведомление (`run_id`) через Redis (best-effort) +
Supabase (надёжно, с ретраями), но само содержимое дайджеста Агент 4 всё равно забирает через
`analysis_digest(run_id)` — очередь/pub-sub несёт только указатель, не сам дайджест.

---

## ✅ D. Доработка Агента 1 — push-хендофф к Агенту 4 — РЕАЛИЗОВАНО (2026-07-09)

> **Статус: сделано.** Коммит `ee038df` (v0.2.6, «Push-хендофф Агент 1 → Агент 4 после
> wizard-запроса»). Точное зеркало паттерна Агента 3: таблица
> `intelligence_agent.agent4_handoff_queue` (миграция `003_agent4_handoff.sql`, применена к живой
> БД), модуль `Code/src/handoff/agent4Handoff.js` (Redis pub/sub `notifications:agent4` + Supabase
> insert с ретраями `[500, 2000, 8000]`мс), вызов `notifyAgent4()` добавлен fire-and-forget сразу
> после сохранения `settings.wizard` на шаге 5 диалога. Живая проверка перед коммитом: реальный
> INSERT в `agent4_handoff_queue` (тестовая строка вычищена) + реальное сообщение получено живым
> Redis-подписчиком на канале `notifications:agent4` с корректным payload
> (`{ event: 'wizard_ready', telegram_id, wizard_hash, mode, timestamp }`). Заодно как побочный
> результат исправлена доком-нестыковка в `Information analysis agent/План разработки/7. Архитектура
> (Бекенд).md` (пункт C выше) — §4.1/§4.3/§6.3 теперь описывают фактическую push+pull гибридную
> модель. Оставлено ниже для истории, не требует повторной работы.

### (обновлено 2026-07-10 — теперь основной путь для MVP, не «на будущее»)

> Написано 2026-07-09, **приоритет повышен 2026-07-10** по прямому указанию пользователя: постфактум-
> сопоставление через прогоны Агента 3 (как было спроектировано изначально) — рабочий, но лишний
> обходной путь. Правильнее, чтобы Агент 1, который точно знает, кто и что запросил, сам будил
> Агента 4 — вместо того, чтобы Агент 4 порознь вычислял, для кого есть работа. Теперь это
> **необходимая доработка для MVP**, не опциональная будущая идея.

### В чём было ограничение старой связки (для истории)

Агент 3 анализирует пакетно (батч по готовности очереди, не по расписанию и не по запросу
конкретного пользователя) — прогон `run_id` не привязан напрямую к конкретному wizard-запросу
пользователя. Постфактум-сопоставление (факт → источник → `raw_job_id` →
`intelligence_agent.search_results.telegram_id` → поиск подходящего wizard-запроса) работало, но
зависело от того, что нужный `telegram_id` вообще попадёт в число источников какого-то прогона
Агента 3, и добавляло задержку ожидания batch-прогона.

### Что нужно сделать — push-хендофф Агент 1 → Агент 4

Точное зеркало уже реализованного и проверенного паттерна Агент 3 → Агент 4 (`agent4Handoff.js`),
только источник — Агент 1:

1. **Новая таблица** `intelligence_agent.agent4_handoff_queue` (Supabase, схема Агента 1 — очередь
   живёт у отправителя, как и `agent3_handoff_queue`/`agent4_handoff_queue` у Агентов 2/3):
   ```sql
   CREATE TABLE intelligence_agent.agent4_handoff_queue (
     id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     telegram_id     BIGINT NOT NULL,
     wizard_hash     TEXT NOT NULL,   -- hash(network+content_type+format+style+description)
     mode            TEXT NOT NULL,   -- 'content' | 'publish'
     attempt_count   INTEGER NOT NULL DEFAULT 0,
     last_attempt_at TIMESTAMPTZ,
     status          TEXT NOT NULL DEFAULT 'pending',
     created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
   );
   ```
2. Сразу по завершении wizard'а (шаг 5, после сохранения `settings.wizard` в `intelligence_agent.
   users`, см. `Code/src/telegram-bot/index.js:891-897`) — публиковать в **Redis**, канал
   `notifications:agent4` (тот же канал, что уже слушает Агент 4 для Агента 3, чтобы не плодить
   лишние подписки — разбор по полю `event`):
   ```json
   { "event": "wizard_ready", "telegram_id": 123, "wizard_hash": "...", "mode": "content", "timestamp": "..." }
   ```
3. Параллельно — запись в таблицу из пункта 1 (надёжный слой). Ретраи `[500мс, 2с, 8с]` на INSERT,
   Redis-публикация — best-effort без ретраев (1-в-1 копия логики `agent4Handoff.js` — переиспользовать
   как образец, не изобретать заново).
4. **In-process буфер с ретраями** внутри Агента 1 — на случай, если сама запись в Redis/Supabase не
   удалась, по аналогии с тем, что уже реализовано у Агента 3.
5. Агент 1 не обновляет `status` после вставки (как и Агент 3) — потребление и дедупликация целиком
   на стороне Агента 4 (уже спроектировано, `content_creation_agent.processed_wizard_requests`, см.
   `Content creation agent/План разработки/07. Архитектура (Бекенд).md`, §6).

### Что это меняет в связке Агент 3 → Агент 4

Не отменяется, но понижается в роли: push-хендофф Агента 3 (пункт B выше) остаётся, но теперь это
**опциональное обогащение трендовыми данными по требованию**, а не сигнал "проснись" для Агента 4.
Полная логика — `Content creation agent/План разработки/07. Архитектура (Бекенд).md`, §4–5.

### Смежная находка — в wizard'е нет явного переключателя "опираться на тренды"

Текущий 5-шаговый wizard (соцсеть/тип/формат/стиль/описание) не имеет отдельного шага/тумблера для
явного запроса опоры на тренды — пользователь может упомянуть это только в свободном тексте шага 5
(`description`). Агент 4 будет вынужден определять намерение эвристически/через LLM по этому тексту,
что менее надёжно, чем явный переключатель. Не блокирует MVP (см. открытый вопрос в «05. ТЗ» Агента
4, §12), но при реальном использовании может понадобиться добавить шестой шаг/кнопку в wizard —
отдельная будущая доработка, не описываю детально здесь, т.к. пока не подтверждена необходимость.

---

## E. Доработка Агента 1 — приём обратного канала от Агента 4 (не реализовано)

> Написано 2026-07-10 в ходе обсуждения квоты хранилища R2 и транспорта отчётов. Зеркало пункта D
> выше, только в обратную сторону: там Агент 1 будил Агента 4, здесь Агент 4 должен уметь достучаться
> до пользователя — а поскольку у Агента 4 нет своего Telegram-бота (осознанное решение, см. «05. ТЗ»,
> §5.4/§9.3 — «не используй токен от ТГ бота 1, всё управление начинается с него»), доставка идёт
> только через Агента 1.

### Что уже сделано на стороне Агента 4

Отправляющая часть готова и протестирована (`Content creation agent/Code/src/delivery/
agent1Notifier.js`, 7 тестов) — точное зеркало уже проверенного `agent4Handoff.js`:

1. Таблица `content_creation_agent.agent1_delivery_queue` (Supabase, схема Агента 4 — очередь живёт
   у отправителя, как и все остальные хендоффы в проекте):
   ```sql
   CREATE TABLE content_creation_agent.agent1_delivery_queue (
     id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     telegram_id           BIGINT NOT NULL,
     message_type          TEXT NOT NULL,   -- 'content_ready' | 'quota_warning' | ...
     generated_content_id  UUID REFERENCES content_creation_agent.generated_content(id), -- nullable
     payload               JSONB NOT NULL DEFAULT '{}'::jsonb,
     attempt_count         INTEGER NOT NULL DEFAULT 0,
     last_attempt_at       TIMESTAMPTZ,
     status                TEXT NOT NULL DEFAULT 'pending',
     created_at            TIMESTAMPTZ NOT NULL DEFAULT now()
   );
   ```
2. После генерации/события — Агент 4 пишет в Redis, канал **`notifications:agent1_from_agent4`**
   (осознанно НЕ `notifications:agent1`, которым уже пользуется Агент 2 → Агент 1 — независимый канал,
   не зависит от формата чужих сообщений):
   ```json
   { "event": "delivery_ready", "telegram_id": 123, "message_type": "content_ready", "timestamp": "..." }
   ```
3. Параллельно — запись в таблицу из пункта 1 (надёжный слой, ретраи `[500мс, 2с, 8с]`).

### Что нужно сделать на стороне Агента 1

1. Подписка на Redis-канал `notifications:agent1_from_agent4` (по аналогии с тем, как Агент 4 уже
   слушает `notifications:agent4` для Агента 1 и Агента 3, см. пункт D выше) — быстрый слой.
2. Поллинг `content_creation_agent.agent1_delivery_queue` (кросс-схемное чтение,
   `.schema('content_creation_agent')`) — надёжный catch-up слой на случай пропуска Redis-события.
3. По `message_type` — разное поведение:
   - `content_ready` — показать пользователю короткий отчёт из `payload` (соцсеть/тип/описание/
     размер + ссылки на R2-файлы) и обработать последующий запрос «прислать файл»/«прислать всё»
     (граница ответственности между «что формирует Агент 4» и «что показывает Агент 1» — уточнить при
     реализации, см. также Слайс 9 Задачника Агента 4).
   - `quota_warning` — показать пользователю предупреждение о лимите хранилища R2 и запросить
     согласие на удаление старого контента; результат согласия/отказа нужно как-то вернуть Агенту 4
     (обратный канал ещё **не спроектирован** — на момент написания этого пункта решена только
     доставка сообщения ДО пользователя, не ответ ОТ пользователя обратно к Агенту 4; вариант — новая
     таблица подтверждений по аналогии с `information_analysis_agent.pending_user_decisions`, но
     детали не проработаны, требуют отдельного решения при реализации).
4. Как и в пункте D — не отмечать `status` в чужой таблице, не обязательно (потребление на стороне
   Агента 1, дедупликация/идемпотентность — решить при реализации по месту, что важнее для Агента 1
   как получателя).

### Статус

Не реализовано (на стороне Агента 1). На стороне Агента 4 квота **готова и работает** (см.
`Content creation agent/План разработки/08. Задачник.md`, раздел «✅ Квота хранилища R2») — порог
общий на весь bucket (10 ГБ, подтверждено документацией Cloudflare), предупреждение конкретного
"тяжёлого" пользователя формируется и уходит через `notifyAgent1` с `message_type: quota_warning`
автоматически при каждой генерации, если порог превышен. Функция фактического удаления
(`deleteGeneratedContent`) тоже готова и протестирована — **но никем не вызывается**, пока не
появится этот самый канал согласия пользователя (пункт 3.«quota_warning» выше). Это сейчас
единственное, что блокирует полный цикл квоты целиком.

---

## Источник

Пункты A/B (реализованы) — прямое следствие брейншторма Агента 4 (`План разработки/04.
Брейншторм.md`, разделы 2 и 3). Пункт C — находка этапа Архитектуры Агента 4 (`План разработки/07.
Архитектура (Бекенд).md`, §5.3), уже после того, как A/B были реализованы. Пункт D — изначально
записан как «на будущее» на этапе Архитектуры, затем **повышен до обязательного для MVP** решением
пользователя 2026-07-10 — актуальная версия архитектуры, учитывающая это, в `План разработки/07.
Архитектура (Бекенд).md`, §4.
